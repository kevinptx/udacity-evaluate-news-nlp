import { updateUI } from "../src/client/js/updateUI";
describe("Checking the function that updates the UI after user form submission", () => {
    test("Testing the updateUI() function", () => {
        expect(updateUI).toBeDefined();
    })
});